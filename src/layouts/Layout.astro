---
interface Props {
  title?: string;
  description?: string;
}
const { title = "Gylsere Januzi - online marketing office", description = "Gylsere Januzi - online marketing office" } = Astro.props;
import "../styles/global.css";
---
<!doctype html>
<html lang="de" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
  </head>
  <body class="bg-[#0b0f19] text-gray-100 min-h-screen flex flex-col font-sans antialiased selection:bg-[#FF6A00] selection:text-white">
    <slot name="header" />
    <main class="flex-grow">
      <slot />
    </main>
    <slot name="footer" />
  </body>
</html>
